---
sidebar_position: 8
---

import { PinCode } from "rizzui";
import Paper from "@site/src/components/paper";

# PinCode

A basic widget for getting pin code for verifications.

```tsx
import { PinCode } from "rizzui";
```

<div className="mb-28" />

## Types

You can change types of input of **PinCode** with property **type**.

<Paper className="grid grid-cols-1 sm:grid-cols-2 gap-4">
  <PinCode
    type="number"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    type="text"
    setValue={function noRefCheck() {}}
  />
</Paper>

```tsx
import { PinCode } from "rizzui";

export default function App() {
  return (
    <>
      <PinCode
        type="number"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        type="text"
        setValue={function noRefCheck() {}}
      />
    </>
  );
}
```

<div className="mb-16" />

## Variants

You can change style of **PinCode** with property [**variant**](#pincode-variants).

<Paper className="flex-row flex-wrap justify-between gap-4">
  <PinCode
    variant="flat"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    variant="outline"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    variant="active"
    setValue={function noRefCheck() {}}
  />
</Paper>

```tsx
import { PinCode } from "rizzui";

export default function App() {
  return (
    <>
      <PinCode
        variant="flat"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        variant="outline"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        variant="active"
        setValue={function noRefCheck() {}}
      />
    </>
  );
}
```

<div className="mb-16" />

## Sizes

You can change style of **PinCode** with property [**size**](#pincode-sizes).

<Paper className="gap-6 justify-start items-start">
  <PinCode
    size="sm"
    variant="flat"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    color="primary"
    variant="flat"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    size="lg"
    variant="flat"
    color="secondary"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    size="xl"
    variant="flat"
    color="info"
    setValue={function noRefCheck() {}}
  />
</Paper>

```tsx
import { PinCode } from "rizzui";

export default function App() {
  return (
    <>
      <PinCode
        size="sm"
        variant="flat"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        color="primary"
        variant="flat"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        size="lg"
        variant="flat"
        color="secondary"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        size="xl"
        variant="flat"
        color="info"
        setValue={function noRefCheck() {}}
      />
    </>
  );
}
```

<div className="mb-16" />

## Rounded

You can change style of **PinCode** with property [**rounded**](#pincode-rounded).

<Paper className="flex-row flex-wrap justify-around items-start gap-8">
  <PinCode
    variant="active"
    rounded="sm"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    variant="active"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    variant="active"
    rounded="lg"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    variant="active"
    rounded="none"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    variant="active"
    rounded="full"
    setValue={function noRefCheck() {}}
  />
</Paper>

```tsx
import { PinCode } from "rizzui";

export default function App() {
  return (
    <>
      <PinCode
        variant="active"
        rounded="sm"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        variant="active"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        variant="active"
        rounded="lg"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        variant="active"
        rounded="none"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        variant="active"
        rounded="full"
        setValue={function noRefCheck() {}}
      />
    </>
  );
}
```

<div className="mb-16" />

## Colors

You can change style of **PinCode** with property [**color**](#pincode-colors).

<Paper className="flex-row flex-wrap justify-between items-start gap-8">
  <PinCode
    variant="flat"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    variant="flat"
    color="primary"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    variant="flat"
    color="secondary"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    variant="flat"
    color="danger"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    variant="flat"
    color="info"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    variant="flat"
    color="success"
    setValue={function noRefCheck() {}}
  />
  <PinCode
    variant="flat"
    color="warning"
    setValue={function noRefCheck() {}}
  />
</Paper>

```tsx
import { PinCode } from "rizzui";

export default function App() {
  return (
    <>
      <PinCode
        variant="flat"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        variant="flat"
        color="primary"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        variant="flat"
        color="secondary"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        variant="flat"
        color="danger"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        variant="flat"
        color="info"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        variant="flat"
        color="success"
        setValue={function noRefCheck() {}}
      />
      <PinCode
        variant="flat"
        color="warning"
        setValue={function noRefCheck() {}}
      />
    </>
  );
}
```

<div className="mb-16" />

## Disabled

You can disable **PinCode** with property **disabled**.

<Paper>
  <PinCode
    disabled
    setValue={function noRefCheck() {}}
  />
</Paper>

```tsx
import { PinCode } from "rizzui";

export default function App() {
  return (
    <PinCode
      disabled
      setValue={function noRefCheck() {}}
    />
  );
}
```

<div className="mb-16" />

## With Mask

You can change style of **PinCode** with property **mask**.

<Paper>
  <PinCode
    mask={true}
    defaultValue={1234}
    setValue={function noRefCheck() {}}
  />
</Paper>

```tsx
import { PinCode } from "rizzui";

export default function App() {
  return (
    <PinCode
      mask={true}
      defaultValue={1234}
      setValue={function noRefCheck() {}}
    />
  );
}
```

<div className="mb-16" />

## With Placeholder

You can change style of **PinCode** with property **placeholder**.

<Paper>
  <PinCode
    placeholder="😀"
    setValue={function noRefCheck() {}}
  />
</Paper>

```tsx
import { PinCode } from "rizzui";

export default function App() {
  return (
    <PinCode
      placeholder="😀"
      setValue={function noRefCheck() {}}
    />
  );
}
```

<div className="mb-16" />

## With Length

You can change style of **PinCode** with property **length**.

<Paper className="justify-around flex-row gap-8 flex-wrap">
  <PinCode
    length={4}
    setValue={function noRefCheck() {}}
  />
  <PinCode
    length={5}
    setValue={function noRefCheck() {}}
  />
</Paper>

```tsx
import { PinCode } from "rizzui";

export default function App() {
  return (
    <>
      <PinCode
        length={4}
        setValue={function noRefCheck() {}}
      />
      <PinCode
        length={5}
        setValue={function noRefCheck() {}}
      />
    </>
  );
}
```

<div className="mb-16" />

## With Error

You can set error message to **PinCode** with property **error**.

<Paper>
  <PinCode
    center={false}
    error="This field is required"
    setValue={function noRefCheck() {}}
  />
</Paper>

```tsx
import { PinCode } from "rizzui";

export default function App() {
  return (
    <PinCode
      center={false}
      error="This field is required"
      setValue={function noRefCheck() {}}
    />
  );
}
```

<div className="mb-20" />

## APIs

<hr />

<div className="mb-10" />

### PinCode Props

Here is the API documentation of the Pin Code component. And the rest of the props of PinCode are the same as the original html input field. You can use props like **disabled**, **placeholder**, **defaultValue** etc.

|     Props      |                 Type                 |                                 Description                                 |   Default   |
| :------------: | :----------------------------------: | :-------------------------------------------------------------------------: | :---------: |
|    setValue    | [PincodeSetvalue](#pincode-setvalue) |                Pass setState to get back the pin code value                 |    \_ \_    |
|      type      |           `number` `text`            |            This Pin Code component only support these two types             |  `"text"`   |
|      mask      |              `boolean`               |                  Mask and unmask to hide and show pin code                  |   `false`   |
|     length     |               `number`               |                             Set pin code length                             |     `4`     |
|     center     |              `boolean`               |                      Make pin code horizontally center                      |   `true`    |
|  placeholder   |               `string`               |                            Set placeholder text                             |    `"o"`    |
|    variant     | [PincodeVariants](#pincode-variants) |                     The variants of the component are:                      | `"outline"` |
|      size      |    [PincodeSizes](#pincode-sizes)    | The size of the component. `"sm"` is equivalent to the dense input styling. | `"DEFAULT"` |
|    rounded     |  [PincodeRounded](#pincode-rounded)  |                          The rounded variants are:                          | `"outlne"`  |
|     color      |   [PincodeColors](#pincode-colors)   |                             Change input color                              | `"DEFAULT"` |
|     error      |               `string`               |                     Show error message using this prop                      |    \_ \_    |
| inputClassName |               `string`               |             Add custom classes for the input filed extra style              |    \_ \_    |
| errorClassName |               `string`               |          This prop allows you to customize the error message style          |    \_ \_    |

<div className="mb-10" />

### Pincode Setvalue

```tsx
type PincodeSetvalue = Dispatch<SetStateAction<string | number | undefined>>;
```

<div className="mb-10" />

### Pincode Variants

```tsx
type PincodeVariants = "flat" | "outline" | "active";
```

<div className="mb-10" />

### Pincode Sizes

```tsx
type PincodeSizes = "sm" | "DEFAULT" | "lg" | "xl";
```

<div className="mb-10" />

### Pincode Rounded

```tsx
type PincodeRounded = "sm" | "DEFAULT" | "lg" | "none" | "full";
```

<div className="mb-10" />

### Pincode Colors

```ts
type PincodeColors =
  | "DEFAULT"
  | "primary"
  | "secondary"
  | "danger"
  | "info"
  | "success"
  | "warning";
```
