import { Empty, EmptyProductBoxIcon, SearchNotFoundIcon } from '@redq/rizz';
import Paper from '@site/src/components/paper';

# Empty

Empty state component. Note: We are highly inspired by the Ant Design empty component -> [https://ant.design/components/empty](https://ant.design/components/empty)

```tsx
import { Empty } from '@redq/rizz';
```

<div className="mb-28" />

## Default

The default style of the **Empty**

<Paper>
	<Empty text="No Data" textClassName="mt-2" />
</Paper>

```tsx
import { Empty } from '@redq/rizz';

export default function App() {
	return <Empty text="No Data" textClassName="mt-2" />;
}
```

<div className="mb-16" />

## No Product

You can use it if your product is **Empty**

<Paper>
	<Empty image={<EmptyProductBoxIcon />} text="No Product Available" />
</Paper>

```tsx
import { Empty, EmptyProductBoxIcon } from '@redq/rizz';

export default function App() {
	return <Empty image={<EmptyProductBoxIcon />} text="No Product Available" />;
}
```

<div className="mb-16" />

## No Result Found

You can use it if your search is **Empty**

<Paper>
	<Empty image={<SearchNotFoundIcon />} text="No Result Found" />
</Paper>

```tsx
import { Empty, SearchNotFoundIcon } from '@redq/rizz';

export default function App() {
	return <Empty image={<SearchNotFoundIcon />} text="No Result Found" />;
}
```

<div className="mb-20" />

## APIs

<hr />

<div className="mb-10" />

### Empty Props

Here is the API documentation of the Empty component.

|         Props         |                Type                |                          Description                           |  Default   |
| :-------------------: | :--------------------------------: | :------------------------------------------------------------: | :--------: |
|         text          |              `string`              |         Set custom text message of the Empty component         |   \_ \_    |
|     textClassName     |              `string`              |               Set custom classes for text style                |   \_ \_    |
|         image         |            `ReactNode`             |               Add custom image or icon component               |   \_ \_    |
|    imageClassName     |              `string`              | Add custom classes on to the image or icon component's wrapper |   \_ \_    |
|   defaultImageWidth   |         `string` `number`          |          Set custom width of the default image / icon          |   `184`    |
|  defaultImageHeight   |         `string` `number`          |         Set custom height of the default image / icon          |   `152`    |
| defaultImageClassName |              `string`              |        Set custom className of the default image / icon        |   \_ \_    |
|        textTag        | [EmptyTextTags](#empty-text-tags)  |                 Here you can use your text tag                 |   `"p"`    |
|       alignment       | [EmptyAlignment](#empty-alignment) |         Set custom text message of the Empty component         | `"center"` |
|       className       |              `string`              |               Add custom classes for extra style               |   \_ \_    |

<div className="mb-10" />

### Empty Text Tags

```ts
type EmptyTextTags = 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6' | 'p';
```

<div className="mb-10" />

### Empty Alignment

```ts
type EmptyAlignment = 'center' | 'inlineStart' | 'inlineEnd';
```
