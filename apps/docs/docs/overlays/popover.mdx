---
sidebar_position: 31
---

import { Popover, Title, Text, Button, ActionIcon, Avatar } from "rizzui";
import Paper from "@site/src/components/paper";
import { TrashIcon } from "@heroicons/react/24/solid";

# Popover

Popover is a simple dialog alert which can have details of the content. It can either have a confirm, cancel button or any action that can be triggered by the user's decision.

```tsx
import { Popover } from "rizzui";
```

<div className="mb-20 md:mb-28" />

## Default

A simple **Popover** component.

<Paper>
  <Popover>
    <Popover.Trigger>
      <Button variant="outline">Open Popover</Button>
    </Popover.Trigger>
    <Popover.Content>
      <Title as="h6" className="!mb-0 font-semibold">
        This is a default popover
      </Title>
      <Text>It is opened when button is clicked. </Text>
    </Popover.Content>
  </Popover>
</Paper>

```tsx
import { Popover, Title, Text, Button } from "rizzui";

export default function App() {
  return (
    <>
      <Popover>
        <Popover.Trigger>
          <Button variant="outline">Open Popover</Button>
        </Popover.Trigger>
        <Popover.Content>
          <Title as="h6">This is a default popover</Title>
          <Text>It is opened when button is clicked. </Text>
        </Popover.Content>
      </Popover>
    </>
  );
}
```

<div className="mb-16" />

## Actionable

You can create actionable **Popover** using state.

<Paper>
  <Popover>
    <Popover.Trigger>
      <ActionIcon variant="outline" color="danger">
        <TrashIcon className="w-5 h-auto" />
      </ActionIcon>
    </Popover.Trigger>
    <Popover.Content>
      {({ setOpen }) => (
        <div className="w-56 py-1">
          <Text as="b" className="font-medium mb-1">
            Delete the task
          </Text>
          <Text className="mb-1 text-sm">
            Are you sure you want to delete the task?
          </Text>
          <div className="flex justify-end gap-3 mb-1">
            <Button size="sm" variant="outline">
              No
            </Button>
            <Button size="sm" onClick={() => setOpen(false)}>
              Yes
            </Button>
          </div>
        </div>
      )}
    </Popover.Content>
  </Popover>
</Paper>

```tsx
import React from "react";
import { Popover, Title, Text, Button } from "rizzui";
import { TrashIcon } from "@heroicons/react/24/solid";

export default function App() {
  return (
    <Popover>
      <Popover.Trigger>
        <Button variant="outline" color="danger">
          <TrashIcon className="w-5 h-auto" />
        </Button>
      </Popover.Trigger>
      <Popover.Content>
        {({ setOpen }) => (
          <div className="w-56 py-1">
            <Title as="h6">Delete the task</Title>
            <Text>Are you sure you want to delete the task?</Text>
            <div className="flex justify-end gap-3 mb-1">
              <Button size="sm" variant="outline">
                No
              </Button>
              <Button size="sm" onClick={() => setOpen(false)}>
                Yes
              </Button>
            </div>
          </div>
        )}
      </Popover.Content>
    </Popover>
  );
}
```

<div className="mb-16" />

## With Custom Content

You can render any thing using our **Popover** component.

<Paper>
  <Popover enableOverlay>
    <Popover.Trigger>
      <div className="w-12 cursor-pointer">
        <Avatar
          name="Jane Doe"
          src="https://randomuser.me/api/portraits/women/40.jpg"
          size="lg"
        />
      </div>
    </Popover.Trigger>
    <Popover.Content>
      {({ setOpen }) => (
        <>
          <div className="mb-3 flex items-center gap-3">
            <Avatar
              name="John Doe"
              src="https://randomuser.me/api/portraits/women/40.jpg"
              className="ring-2 ring-blue ring-offset-2"
              size="lg"
            />
            <div className="text-left">
              <h1 className="text-base font-medium text-gray-900">
                Fred Chaparro
              </h1>
              <p className="text-sm font-light text-gray-500">@fredchaparro</p>
            </div>
          </div>
          <div className="max-w-[240px] text-left text-sm">
            <h2 className="text-gray-600">
              Full-stack Developer, love to work with @redq. 🎉{" "}
            </h2>
            <span className="mt-3 inline-flex gap-3 text-gray-400">
              <h2>
                <span className="font-medium">8</span> Following
              </h2>
              <h2>
                <span className="font-medium">10.5k</span> Followers
              </h2>
            </span>
          </div>
          <Button
            className="mt-4 w-full"
            variant="solid"
            color="info"
            size="sm"
            onClick={() => setOpen(false)}
          >
            Follow
          </Button>
        </>
      )}
    </Popover.Content>
  </Popover>
</Paper>

```tsx
import React from "react";
import { Popover, Button, Avatar } from "rizzui";

export default function App() {
  return (
    <div className="h-44">
      <Popover
        content={({ setOpen }) => (
          <>
            <div className="mb-3 flex items-center gap-3">
              <Avatar
                name="John Doe"
                src="https://images.unsplash.com/photo-1555952517-2e8e729e0b44?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=928&q=80"
                className="ring-2 ring-blue ring-offset-2"
                size="lg"
              />
              <div className="text-left">
                <h1 className="text-base font-medium text-gray-900">
                  Fred Chaparro
                </h1>
                <p className="text-sm font-light text-gray-500">
                  @fredchaparro
                </p>
              </div>
            </div>
            <div className="max-w-[240px] text-left text-sm">
              <h2 className="text-gray-600">
                Full-stack Developer, love to work with @redq. 🎉{" "}
              </h2>
              <span className="mt-3 inline-flex gap-3 text-gray-400">
                <h2>
                  <span className="font-medium">8</span> Following
                </h2>
                <h2>
                  <span className="font-medium">10.5k</span> Followers
                </h2>
              </span>
            </div>
            <Button
              className="mt-4 w-full"
              variant="solid"
              color="info"
              size="sm"
              onClick={() => setOpen(false)}
            >
              Follow
            </Button>
          </>
        )}
        placement="bottom-start"
      >
        <div className="w-14 cursor-pointer">
          <Avatar
            name="John Doe"
            src="https://images.unsplash.com/photo-1555952517-2e8e729e0b44?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=928&q=80"
            size="lg"
          />
        </div>
      </Popover>
    </div>
  );
}
```

<div className="mb-20" />

## API Reference

<hr />

<div className="mb-10" />

### Popover Props

Here is the API documentation of the Popover component.

<div className="overflow-x-auto">

|         Props         |                   Type                   |                     Description                     |   Default   |
| :-------------------: | :--------------------------------------: | :-------------------------------------------------: | :---------: |
|       children        |   [PopoverChildren](#popover-children)   |        Pass children which will have tooltip        |    \_ \_    |
|         size          |      [PopoverSizes](#popover-sizes)      |            Supported Tooltip sizes are:             | `"DEFAULT"` |
|        rounded        |    [PopoverRounded](#popover-rounded)    |              The rounded variants are:              | `"DEFAULT"` |
|       placement       | [PopoverPlacements](#popover-placements) |          Supported Tooltip Placements are:          |    \_ \_    |
|        shadow         |    [PopoverShadow](#popover-shadows)     |           Supported tooltip shadows are:            | `"DEFAULT"` |
|       className       |                 `string`                 | Add custom classes for Tooltip container or content |    \_ \_    |
|        isOpen         |                `boolean`                 |    Pass open state to open the floating element     |    \_ \_    |
|       setIsOpen       |   `Dispatch<SetStateAction<boolean>>`    |           Pass setOpen to set open value            |    \_ \_    |
|        content        |               `ReactNode`                |                 Content for tooltip                 |    \_ \_    |
| tooltipArrowClassName |                 `string`                 |        Add custom classes for Tooltip arrow         |    \_ \_    |
|       showArrow       |                `boolean`                 |   Whether tooltip arrow should be shown or hidden   |    \_ \_    |

</div>

<div className="mb-10" />

### Popover Children

```ts
type PopoverChildren = Element & { ref?: RefObject<any> | undefined };
```

<div className="mb-10" />

### Popover Sizes

```ts
type PopoverSizes = "sm" | "DEFAULT" | "lg" | "xl";
```

<div className="mb-10" />

### Popover Rounded

```ts
type PopoverRounded = "sm" | "DEFAULT" | "lg" | "none" | "pill";
```

<div className="mb-10" />

### Popover Placements

```ts
type PopoverPlacements =
  | "left"
  | "right"
  | "top"
  | "bottom"
  | "left-start"
  | "left-end"
  | "right-start"
  | "right-end"
  | "top-start"
  | "top-end"
  | "bottom-start"
  | "bottom-end";
```

<div className="mb-10" />

### Popover Shadows

```ts
type PopoverShadow = "sm" | "DEFAULT" | "lg" | "xl";
```
