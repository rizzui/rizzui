---
sidebar_position: 4
---

import { Button } from 'rizzui/button';
import { Badge } from 'rizzui/badge';
import { ActionIcon } from 'rizzui/action-icon';
import { Input } from 'rizzui/input';
import { Text, Title } from 'rizzui/typography';
import { Checkbox } from 'rizzui/checkbox';
import { Radio } from 'rizzui/radio';
import { Switch } from 'rizzui/switch';
import { Avatar } from 'rizzui/avatar';
import { Alert } from 'rizzui/alert';
import {
  BellIcon,
  BoltIcon,
  HeartIcon,
  HandThumbUpIcon,
  ChatBubbleOvalLeftIcon,
  UserIcon,
  CheckCircleIcon,
  ExclamationTriangleIcon,
  InformationCircleIcon,
} from '@heroicons/react/24/outline';
import Paper from '@site/src/components/paper';
import ColorTokens from '@site/src/components/color-tokens';
import ThemeWrapper from '@site/src/components/theme-wrapper';

# Theming

Theming is essential for creating visually appealing and accessible applications. RizzUI 2.0 uses **Tailwind CSS v4** with the modern **OKLCH color space** to provide perceptually uniform colors that look consistent across different screens and lighting conditions.

## Why OKLCH Color Space?

OKLCH (Lightness, Chroma, Hue) offers several advantages over traditional RGB/HEX:

- **Perceptual Uniformity** - Colors with the same lightness value appear equally bright to human eyes
- **Better Accessibility** - Easier to create color palettes that meet WCAG contrast requirements
- **Predictable Color Manipulation** - Adjusting lightness values produces intuitive results
- **Future-Proof** - Native browser support with automatic fallbacks for older browsers

## Theme Tokens

Customize your color tokens for both light and dark modes. Click on any color box below to select a new color, then copy the generated CSS variable to your **globals.css** file.

<ThemeWrapper>
<ColorTokens />

## Live Preview

Try changing the colors above and see how they affect the components below in real-time. This preview demonstrates how theme tokens control colors, borders, backgrounds, and typography across different component types:

<Paper disableShowCodeBtn>
  <div className="space-y-16">
    {/* Buttons Section */}
    <div>
      <Text className="text-text-primary !mb-4 !text-base">Buttons</Text>
      <div className="flex flex-wrap items-center gap-6 mb-5">
        <Button>Primary</Button>
        <Button variant="outline">Outline</Button>
        <Button variant="flat">Flat</Button>
        <Button variant="text">Text</Button>
        <Button variant="danger">Danger</Button>
        <Button disabled>Disabled</Button>
      </div>
      <div className="flex items-center gap-6">
        <ActionIcon>
          <HeartIcon className="h-5 w-5" />
        </ActionIcon>
        <ActionIcon variant="outline">
          <BellIcon className="h-5 w-5" />
        </ActionIcon>
        <ActionIcon variant="flat">
          <BoltIcon className="h-5 w-5" />
        </ActionIcon>
        <ActionIcon variant="text">
          <HandThumbUpIcon className="h-5 w-5" />
        </ActionIcon>
        <ActionIcon variant="danger">
          <ChatBubbleOvalLeftIcon className="h-5 w-5" />
        </ActionIcon>
        <ActionIcon disabled>
          <UserIcon className="h-5 w-5" />
        </ActionIcon>
      </div>
    </div>

    {/* Badges Section */}
    <div>
      <Text className="text-text-primary !mb-4 !text-base">Status Badges</Text>
      <div className="flex flex-wrap items-center gap-2">
        <Badge>Default</Badge>
        <Badge color="secondary">Secondary</Badge>
        <Badge color="danger">Danger</Badge>
        <Badge color="warning">Warning</Badge>
        <Badge color="info">Info</Badge>
        <Badge color="success">Success</Badge>
      </div>
    </div>

    {/* Form Inputs Section */}
    <div>
      <Text className="text-text-primary !mb-4 !text-base">Form Inputs</Text>
      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        <Input label="Email" placeholder="your@email.com" type="email" />
        <Input label="Password" placeholder="Enter password" type="password" />
      </div>
      <div className="flex flex-wrap items-center gap-6 pt-2">
        <Checkbox label="Checkbox" defaultChecked />
        <Radio label="Radio" name="demo" defaultChecked />
        <Switch label="Switch" defaultChecked />
      </div>
    </div>

    {/* Typography Section */}
    <div>
      <Text className="text-text-primary !mb-4 !text-base">Typography</Text>
      <div className="space-y-2">
        <Title as="h1" className="text-text-primary">Heading 1 - Primary Text</Title>
        <Text className="text-text-secondary">Body text using secondary text color. This demonstrates how text-secondary token affects readability.</Text>
        <Text className="text-muted-foreground text-sm">Muted text for less prominent information.</Text>
      </div>
    </div>

    {/* Cards & Containers Section */}
    <div>
      <Text className="text-text-primary !mb-4 !text-base">Cards & Containers</Text>
      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div className="p-5 rounded-lg border border-border bg-background">
          <Title as="h5" className="text-text-primary mb-2">Default Card</Title>
          <Text className="text-text-secondary text-sm">
            Uses background, border-color, and text tokens for a clean card design.
          </Text>
        </div>
        <div className="p-5 rounded-lg bg-muted/50 border border-border">
          <Title as="h5" className="text-text-primary mb-2">Muted Card</Title>
          <Text className="text-text-secondary text-sm">
            Demonstrates muted background with border-radius token for rounded corners.
          </Text>
        </div>
      </div>
    </div>

    {/* Alerts Section */}
    <div>
      <Text className="text-text-primary !mb-4 !text-base">Alerts & Feedback</Text>
      <div className="space-y-3">
        <Alert color='info'>
          <Text className="text-sm">Info alert using info color tokens.</Text>
        </Alert>
        <Alert color='success'>
          <Text className="text-sm">Success alert using success color tokens.</Text>
        </Alert>
        <Alert color='warning'>
          <Text className="text-sm">Warning alert using warning color tokens.</Text>
        </Alert>
        <Alert color='danger'>
          <Text className="text-sm">Danger alert using danger color tokens.</Text>
        </Alert>
      </div>
    </div>

    {/* User Interface Elements */}
    <div>
      <Text className="text-text-primary !mb-4 !text-base">User Interface</Text>
      <div className="flex flex-wrap items-center gap-4">
        <Avatar name="John Doe" src="" />
        <Avatar name="Jane Smith" src="" color="secondary" />
        <Avatar name="Admin" src="" color="red" />
        <div className="flex items-center gap-2 px-4 py-2 rounded-lg border border-border bg-muted/30">
          <UserIcon className="h-5 w-5 text-text-secondary" />
          <Text className="text-text-primary text-sm font-medium">User Profile</Text>
        </div>
      </div>
    </div>

    {/* Border Radius Demo */}
    <div>
      <Text className="text-text-primary !mb-4 !text-base">Border Radius</Text>
      <div className="flex flex-wrap items-center gap-4">
        <div className="w-16 h-16 bg-primary rounded-[var(--border-radius)] flex items-center justify-center">
          <Text className="text-primary-foreground text-xs">Radius</Text>
        </div>
        <div className="px-4 py-2 bg-muted rounded-[var(--border-radius)]">
          <Text className="text-text-primary text-sm">Button-like</Text>
        </div>
        <div className="px-4 py-2 border-2 border-border rounded-[var(--border-radius)]">
          <Text className="text-text-primary text-sm">Bordered</Text>
        </div>
      </div>
    </div>

  </div>
</Paper>

## Customization Tips

To customize your brand colors, update the CSS variables in your `globals.css`:

```css
:root {
  /* Change primary brand color */
  --primary: oklch(50% 0.2 250); /* Your custom color */

  /* Adjust secondary accent color */
  --secondary: oklch(60% 0.25 300);
}
```

### Creating Custom Color Schemes

Use the [OKLCH color picker](https://oklch.com/) to generate consistent color palettes. Keep these guidelines in mind:

- **Lightness (L)**: 0% (black) to 100% (white) - controls brightness
- **Chroma (C)**: 0 (gray) to 0.4+ (vibrant) - controls color intensity
- **Hue (H)**: 0-360 degrees - controls the color itself

For accessible designs, maintain at least a 4.5:1 contrast ratio between text and backgrounds.

</ThemeWrapper>
